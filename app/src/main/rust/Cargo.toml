[package]
name = "filebridge_rust"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"

# 面向体积与性能的发布产物
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true