[package]
name = "filebridge_core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "filebridge_core"
crate-type = ["cdylib"]

[dependencies]

# 面向体积与性能的发布产物
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true